services:
  backend:
    build: ./backend
    ports:
      - "5001:5000"
    volumes:
      - ./backend/jlpt.db:/app/jlpt.db
    environment:
      - FLASK_ENV=development

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend